# Raw Scope Segments

## Summary
- Main scope locator: `{{ main_scope_locator.mode }}{% if main_scope_locator.mode == "line_range" %}:{{ main_scope_locator.line_start }}-{{ main_scope_locator.line_end }}{% endif %}`
- Aux scope count: {{ aux_scopes|length }}
- Segment count: {{ segments|length }}

{% if aux_scopes %}
## Aux Scope Buckets
{% for item in aux_scopes %}
- `{{ item.aux_id }}` | {{ item.label }} | {{ item.purpose }} | `{{ item.locator.mode }}{% if item.locator.mode == "line_range" %}:{{ item.locator.line_start }}-{{ item.locator.line_end }}{% endif %}`
{% endfor %}
{% endif %}

## Raw Blocks
{% if segments %}
{% for item in segments %}
- [{{ item.segment_order }}] {{ item.segment_id }} | {{ item.segment_kind }} | role={{ item.scope_role }} | bucket=`{{ item.scope_bucket_id }}` | label={{ item.scope_label }} | {{ item.heading_context or "（无 heading）" }} | lines={{ item.source_line_start }}-{{ item.source_line_end }} | chars={{ item.char_count }}

  {{ item.source_text_preview }}
{% endfor %}
{% else %}
- （空）
{% endif %}
